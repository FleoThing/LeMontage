failure-threshold: warning
ignored:
  # Debian package versions are intentionally left to the base image repository.
  - DL3008
  # The Dockerfile installs the local package, which Hadolint cannot pin.
  - DL3013
